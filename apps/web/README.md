# PlanForge Web

Astro SSR demo with Svelte islands and Threlte.

## Env

- `PUBLIC_API_BASE_URL` (default: http://localhost:3001)
- `PUBLIC_ORCHESTRATOR_BASE_URL` (default: http://localhost:3002)

## Run

```bash
bun --filter @planforge/web run dev
```
